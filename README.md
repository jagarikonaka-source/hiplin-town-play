# HIPLIN TOWN

Public Unity WebGL player. This repository contains deployment files only.

The Pages workflow publishes `site/` and restores the large movie from verified chunks.
Push an updated WebGL build to `main` to publish a new version.
